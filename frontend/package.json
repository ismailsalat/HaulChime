{
  "name": "haulchime-frontend",
  "version": "0.3.0",
  "private": true,
  "description": "HaulChime static marketing site and quote form.",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx --yes serve dist -l ${PORT:-8080}"
  }
}
